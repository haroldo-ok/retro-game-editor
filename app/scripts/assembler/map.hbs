;==============================================================
; Map set # {{position}}: {{name}}
;==============================================================
Map_{{position}}:
Map_{{formattedId}}:
  ; Map data
{{#each tiles}}
  db {{this}}
{{/each}}
  ; Pointer to map's tileset
  dw TileSet_{{formattedTileSetId}}
